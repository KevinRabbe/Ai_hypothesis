name: Population-compute development preflight

on:
  push:
    branches:
      - agent/population-compute-development-preflight-v0
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  preflight:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    env:
      PYTHONPATH: .

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install CPU Torch
        run: |
          python -m pip install --disable-pip-version-check torch --index-url https://download.pytorch.org/whl/cpu
          python -c "import torch; print('torch', torch.__version__)"

      - name: Run full CPU development seed 0
        run: |
          python -m ai_hypothesis.population_compute.run_relay_scaling \
            --training-seed 0 \
            --device cpu \
            --steps 2000 \
            --train-batch-size 64 \
            --evaluation-world-count 1000 \
            --evaluation-batch-size 64 \
            --state-width 64 \
            --message-width 24 \
            --output-dir results/population_compute_development_cpu_seed0

      - name: Upload development artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: population-compute-development-cpu-seed0
          path: results/population_compute_development_cpu_seed0
          if-no-files-found: error
